---
description: Reuse, decouple, SOLID-oriented boundaries, shared design over reinvention
alwaysApply: true
---

# 工程自覺：少造輪、解耦、邊界清楚

在改動或新增程式前，先當成資深工程師自問並落實。

## 重用與範圍

1. **專案裡是否已有** 相同或類似行為（函式、service、middleware、型別、常數、錯誤處理、HTTP 客戶端封裝）？有則**擴充或呼叫**，不要平行再寫一套。
2. **是否與既有風格一致**（命名、錯誤回傳、日誌、async 風格、目錄職責）？新程式應讀起來像同一個 codebase。
3. **新抽象是否值得**：若只是多一兩行呼叫，不要為「優雅」硬抽共用層；若已在第三處複製同樣邏輯，才考慮抽出共用。
4. **範圍收斂**：只改任務需要的檔案與行數；不順手大 refactor、不複製貼上整段再微調。

## 解耦與職責（呼應 SOLID，實務取向）

- **單一職責**：路由／handler 薄；I/O、商業規則、對外 API 形狀分開。一個模組改動理由應盡量單一。
- **依賴方向**：下層（純邏輯、存取）不依賴上層（HTTP、CLI）。需要交換實作時，用**小介面或函式型別**在邊界注入，避免 concrete 類型滲透全專案。
- **開放／封閉**：擴充行為優先**加函式或策略參數**，少改既有核心分支樹。
- **介面隔離**：呼叫端只依賴它用到的能力，不要塞「萬用 context」或胖型別。
- **里氏替換**：同一抽象的多個實作替換時，不應破壞呼叫端對行為與錯誤語意的合理假設。
- **依賴反轉／可替換性**：細節依賴抽象；外部服務（Civitai、Comfy、Ollama、檔案）透過**集中封裝**呼叫，避免在多處散落同樣 URL／序列化細節。

## 共用化設計

- **同一概念一處定義**：常數、錯誤碼、DTO 映射、重試／逾時策略，避免多檔各寫一份。
- **共用層要有邊界**：抽到 `services` / `lib` 等時，命名與匯出要讓「誰能 import 誰」一眼可懂；不為共用而製造循環依賴。

目標：**不重複解同一題**、**依賴單向且可測**，並在每次變更中維持可讀性與可維護性。
